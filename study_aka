# how to install the software
    sudo apt-get install software
    
# how to reinstall the software
    sudo dpkg --purge software
#list the software
    dpkg -l
#find out the process
    ps aux|grep software
#make patch
    diff -u a b>ab.diff
    patch a<ab.diff
#delete edition
    git checkout SerialNumber
    ##return the delete edition
        git branch
        git checkout master
#show code on net
    vim h.md
    markdown h.md >h.html
    firefox h.md
#how to go to the other user's PC
    ssh PC@IP address
#delete the new edition
    git reset --hard HEAD^
#edit the edition by another person
    git config --global user.name "name"
    vim .gitconfig
#
